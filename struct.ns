struct Inner {
    const a: int;
}

struct Test {
    const i: Inner;
}

mod Test {
    fn new(i: int) {
        return Test(Inner(i));
    }
}

fn main(args: string[]): void {
    let t: Test = Test.new(5);
    print(t.i.a);
}