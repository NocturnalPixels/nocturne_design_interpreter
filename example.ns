/*

Testing File

*/

fn main(args: string[]): void {
    for (i: int = 0; i < 100; i++) {
        let s: string = "";

        if (i % 3 == 0) {
            s += "Fizz";
        }

        if (i % 5 == 0) {
            s += "Buzz";
        }

        if (i % 3 != 0 && i % 5 != 0) {
            s += i;
        }

        print(s);
    }
}